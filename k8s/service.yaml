apiVersion: v1
kind: Service
metadata:
  name: cicd-demo
  namespace: default
spec:
  type: NodePort
  selector:
    app: cicd-demo
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30080
